customtkinter>=5.2.2
PyYAML>=6.0.1
tkinterdnd2>=0.4.2
packaging>=23.0
pywin32>=306; sys_platform == "win32"

# Parser GRF (Shopee Full) - decodifica etiquetas rasterizadas (preview) e
# le o SKU Shopee pelo QR code de cada sticker.
Pillow>=10.1.0
pyzbar>=0.1.9
# Dependencias do sistema (instalar separadamente):
#   Linux:   apt install libzbar0
#   Windows: o pyzbar ja inclui libzbar.dll empacotada.
#
# Interpretacao de ZPL -> imagem (src/core/zpl_renderer.py): nao usa pacote
# Python -- chama o renderer Node via subprocess. Requer Node.js v18+ instalado
# e `cd renderer && npm install` (pacote zpl-renderer-js). Veja o README.
